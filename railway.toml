[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python server.py"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"

[[deploy.envs]]
PORT = { default = "8899" }
HOST = { default = "0.0.0.0" }
